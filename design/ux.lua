local ux = {}

local menus = {}

local defaultmenu = {
    orientation = "vertical",
    x = 0,
    y = 0,
    
}

function ux:configure(config)
    assert(type(config)=="table","UX configured with invalid configure table!")



end




return ux